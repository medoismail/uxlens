"use client";

/**
 * Hero background — intentionally empty/flat.
 * Linear-style: the background IS the background.
 * No glows, no gradients, no noise. Color is earned.
 */
export function HeroBackground() {
  return null;
}
